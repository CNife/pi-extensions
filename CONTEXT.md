# pi-extensions

pi 的扩展集合，提供 AI 开发工作流——围绕变更目录、分阶段命令和领域文档的协作体系。

## 项目用语

**变更**：
一个完整的开发工作单元，由 `YYYYMMDD-<简写>` 标识，包含方案、任务、决策和全流程日志。
_避免_：PRD、需求、工单、issue

**任务**：
变更内的一个垂直切片，端到端穿透所有层（方案→代码→测试），可独立验证。
_避免_：子任务、工作项、ticket、subtask（任务本身就是最小可执行单元）

**grill**：
`/grill` 命令——逐条追问用户以澄清变更范围和用语，对照 `CONTEXT.md` 和 `docs/adr/` 发现冲突，用语确定即时更新 CONTEXT.md，值得记录的决策创建 ADR。
_避免_：需求访谈、沟通对齐（太模糊）

**变更方案** (`plan.md`)：
grill 完成后，基于已澄清的结论一次性写入的变更方案文档。
_避免_：PRD、需求文档、spec

**变更日志** (`change.md`)：
变更目录下的追加写入文件，记录 grill→plan→拆解→编码→审查的完整过程，按 v1→v2→... 版本号顺延。

**领域文档对齐**：
grill 阶段的核心动作——对照 `CONTEXT.md` 和 `docs/adr/` 打磨用语、消除歧义，而非用模糊词蒙混过关。
_避免_：术语统一、概念对齐（太模糊）

**架构决策记录** (`adr`)：
记录一个难以逆转的技术决策——背景、决定、理由。变更内产出后，变更完成时同步到根 `docs/adr/`。
_避免_：设计文档、技术方案（ADR 只记决定，不记过程）

**垂直切片**：
一种任务拆解策略：每个任务是一段端到端的功能路径（从入口到产出），而非按技术层水平切分（如"写 DAO 层"）。
_避免_：模块拆分、水平拆分

**红绿重构**：
TDD 的三拍循环：先写失败的测试（红）→ 写最小实现让它通过（绿）→ 重构代码（保持绿色）。

**架构审查**：
`/improve-architecture` 命令——手动触发，扫描代码库对照 CONTEXT.md 和 ADR 发现架构改进机会，产出列表供用户选择，确认后创建新的变更方案。

**缩放**：
`/zoom-out` 命令——在不熟悉的代码区域提升一个抽象层级，给出相关模块和调用者的全景地图，使用项目用语。

**原型**：
`/prototype` 命令——独立辅助技能，不入流水线。构建可丢弃原型验证代码层不确定性（状态机逻辑、业务规则），结论写入 plan.md。

**grill-me**：
`/grill-me` 命令——独立辅助技能，不入流水线。和 `/grill` 一样逐条追问，但不绑定变更目录、不写任何文件。日常想理清任何想法时用。
_避免_：头脑风暴（太模糊，grill-me 是有结构的追问）

**代码审查**：
`/review-code` 命令——tdd 完成后 AI 先审：范围偏差、硬性阻断、模式补全、文档债。安全自动修复直接改，需确认的列出等判断，架构问题留给 plannotator 人类审。最后追问根因，值得改进的交给 `/improve-architecture`。

## Example Dialogue

> **Dev**: 有一个订单取消的需求，搞个变更。
>
> **Dev**: 好，先 grill。
> **Agent**: `/grill` 启动。先确认——"取消"在你的语境里是指用户主动取消，还是包含系统超时取消？
>
> **Dev**: 目前只做用户主动取消。
> **Agent**: 把这个用语写进 CONTEXT.md。然后——取消后是直接退款还是先冻结？
>
> **Dev**: 先冻结，人工审核后退款。
> **Agent**: 这是一个架构决策——退款和取消解耦。值得记一条 ADR。grill 完成，进入 `/plan` 写方案。
